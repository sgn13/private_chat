import React from "react";
import { Card } from "antd";

const { Meta } = Card;

const Home = () => {
  return (
    <div className="container my-4 text-center">
      <h4>CHAT APP</h4>
    </div>
  );
};

export default Home;
